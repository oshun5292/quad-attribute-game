// sw.test.js — Service Worker のテスト（将来の拡張用）
import { describe, test } from 'vitest';

describe('ServiceWorker (placeholder)', () => {
  test('テストスイートが存在する', () => {
    // 将来の Service Worker テストのためのプレースホルダー
  });
});
